fastapi>=0.110,<1.0
uvicorn[standard]>=0.27,<1.0
pydantic>=2.6,<3.0
anthropic>=0.34,<1.0
numpy>=1.26,<3.0
pypdf>=4.0,<6.0
# Local embeddings (default backend). CPU-only torch to keep the image small.
sentence-transformers>=3.0,<5.0
torch>=2.2,<3.0
# Optional OpenAI embedder (EMBED_BACKEND=openai)
openai>=1.40,<2.0
# Cloudflare Access JWT verification
python-jose[cryptography]>=3.3,<4.0
httpx>=0.27,<1.0
