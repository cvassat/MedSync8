# Minimal deps for running the backend test suite.
# Excludes torch + sentence-transformers (tests use a stub embedder, and
# downloading a 130MB model every CI run is wasteful). The Docker build
# installs the full requirements.txt and prefetches the model weights.
fastapi>=0.110,<1.0
pydantic>=2.6,<3.0
numpy>=1.26,<3.0
pypdf>=4.0,<6.0
anthropic>=0.34,<1.0
openai>=1.40,<2.0
python-jose[cryptography]>=3.3,<4.0
httpx>=0.27,<1.0
pytest>=8.0
